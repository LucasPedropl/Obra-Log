{
	"version": "0.2.0",
	"configurations": [
		{
			"name": "Iniciar API + Front (Local)",
			"type": "pwa-chrome",
			"request": "launch",
			"url": "http://localhost:3000",
			"webRoot": "${workspaceFolder}/src",
			"preLaunchTask": "Start All",
			"presentation": {
				"hidden": false
			}
		}
	]
}
