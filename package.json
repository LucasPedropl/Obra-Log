{
  "name": "obralog-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:erp": "npm run dev --workspace=@obralog/erp",
    "dev:admin": "npm run dev --workspace=@obralog/admin",
    "dev:api": "npm run dev --workspace=@obralog/api"
  }
}